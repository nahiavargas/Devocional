<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Devocional</title> <!-- solo en la pestaña -->

<style>
  body {
    font-family: Arial, sans-serif;
    background-color: #fdf6e3;
    color: #333;
    margin: 0;
    padding: 20px;
    text-align: center;
  }

  h1 {
    color: #2c3e50;
    margin-bottom: 15px;
  }

  iframe {
    width: 100%;
    max-width: 560px;
    height: 315px;
    border: none;
    margin-bottom: 20px;
  }

  h2 {
    color: #2c3e50;
    font-weight: normal;
    margin-top: 0;
  }

  /* Botones sociales al costado derecho, vertical */
  .social-bar {
    position: fixed;
    top: 50%;
    right: 0;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    gap: 10px;
    z-index: 1000;
  }

  .social-bar a {
    background-color: #2c3e50;
    color: white;
    text-decoration: none;
    padding: 10px 15px;
    border-radius: 5px 0 0 5px;
    font-weight: bold;
    transition: transform 0.3s ease, background-color 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .social-bar a:hover {
    background-color: #1a2733;
    transform: translateX(-10px);
  }
</style>
</head>

<body>

  <h1>Blog</h1>

  <!-- Video embebido -->
  <iframe 
    src="https://www.youtube.com/embed/TU_VIDEO_AQUI" 
    title="Video"
    allowfullscreen
  ></iframe>

  <h2>Un mensaje reflexivo para el día</h2>

  <!-- Botones sociales al costado -->
  <div class="social-bar">
    <a href="https://www.instagram.com/" target="_blank" aria-label="Instagram">Instagram</a>
    <a href="https://www.facebook.com/" target="_blank" aria-label="Facebook">Facebook</a>
    <a href="https://www.youtube.com/" target="_blank" aria-label="YouTube">YouTube</a>
    <a href="https://www.whatsapp.com/" target="_blank" aria-label="WhatsApp">WhatsApp</a>
  </div>

</body>
</html>
