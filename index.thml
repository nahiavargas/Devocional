<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Devocional</title>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap');

    :root {
      --bg: #f5f7fa;
      --primary: #2c3e50;
      --accent: #3498db;
      --light: #ffffff;
      --shadow: rgba(0,0,0,0.1);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Roboto', sans-serif;
      background-color: var(--bg);
      color: var(--primary);
      overflow-x: hidden;
    }

    header {
      width: 100%;
      padding: 40px 20px;
      text-align: center;
    }

    header h1 {
      font-size: 3em;
      font-weight: 700;
      color: var(--primary);
    }

    .description {
      max-width: 700px;
      margin: 20px auto;
      font-weight: 300;
      font-size: 1.2em;
      line-height: 1.5;
      color: #555;
    }

    .video-wrapper {
      display: flex;
      justify-content: center;
      margin: 40px 0;
    }

    .video-wrapper iframe {
      width: 100%;
      max-width: 640px;
      height: 360px;
      border-radius: 12px;
      box-shadow: 0 10px 30px var(--shadow);
      transition: transform 0.3s ease;
    }

    .video-wrapper iframe:hover {
      transform: scale(1.02);
    }

    .message {
      text-align: center;
      font-size: 1.8em;
      font-weight: 500;
      margin-bottom: 80px;
      color: var(--primary);
    }

    .sidebar {
      position: fixed;
      top: 50%;
      left: -200px;
      transform: translateY(-50%);
      width: 200px;
      display: flex;
      flex-direction: column;
      gap: 20px;
      transition: left 0.4s ease;
    }

    .sidebar.visible {
      left: 0;
    }

    .sidebar a {
      background-color: var(--accent);
      color: var(--light);
      text-decoration: none;
      padding: 12px 20px;
      border-radius: 0 20px 20px 0;
      font-weight: 500;
      transition: background-color 0.3s ease;
    }

    .sidebar a:hover {
      background-color: darken(var(--accent), 10%);
    }

    .toggle-sidebar {
      position: fixed;
      top: 20px;
      left: 20px;
      background-color: var(--accent);
      color: var(--light);
      padding: 10px 15px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1em;
      font-weight: bold;
      box-shadow: 0 4px 10px var(--shadow);
    }

    @media (max-width: 768px) {
      .video-wrapper iframe {
        height: 240px;
      }
      .sidebar {
        display: none;
      }
      .toggle-sidebar {
        display: none;
      }
    }
  </style>
</head>
<body>

  <button class="toggle-sidebar" onclick="document.querySelector('.sidebar').classList.toggle('visible')">
    Menú
  </button>

  <div class="sidebar">
    <a href="https://www.instagram.com/" target="_blank">Instagram</a>
    <a href="https://www.facebook.com/" target="_blank">Facebook</a>
    <a href="https://www.youtube.com/" target="_blank">YouTube</a>
    <a href="https://www.whatsapp.com/" target="_blank">WhatsApp</a>
  </div>

  <header>
    <h1>Blog</h1>
    <p class="description">
      Un espacio moderno con video, reflexiones y conexión directa a redes sociales.
    </p>
  </header>

  <div class="video-wrapper">
    <iframe src="https://www.youtube.com/embed/TU_VIDEO_AQUI" title="Video reflexivo" allowfullscreen></iframe>
  </div>

  <div class="message">
    Aquí va un mensaje reflexivo que inspire al visitante.
  </div>

</body>
</html>
